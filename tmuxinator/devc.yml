# ~/.tmuxinator/dev4.yml

name: dev
root: ~/

windows:
  - dev:
      layout: even-horizontal
      panes:
        - 1:
          - ssh dev
          - cd frontend
          - rails c
        - 2:
          - ssh dev
          - cd backend
          - bin/logikcull console -E development
